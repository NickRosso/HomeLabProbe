services:
  probapi:
    build: .
    container_name: probapi
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    restart: unless-stopped

  homelab-tests: 
    build: . 
    container_name: homelab-tests
    environment: 
      - PYTHONPATH=/code
    volumes: 
      - .:/code 
    command: pytest -vv --cov=app --cov-report=html